sudo: required
services:
  - docker

script:
  - docker build -t react-app .

before_deploy:
  - docker login -u mannydesigns -p vVc9xAvUZK81whlk 
  - git_sha="$(git rev-parse --short HEAD)"
  - docker tag react-app

deploy:
  provider: script
  script: docker push mannydesigns/react-example1
  on:
    branch: master